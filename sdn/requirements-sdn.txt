# BAZTA — SDN Controller Dependencies (Deprecated)
#
# Note: The Python-based SDN controller (Ryu/OS-Ken) has been replaced by the Java-based ONOS controller.
# You no longer need to install these Python packages.

# requests>=2.33.1 (Kept for historical reference if building external python scripts)
requests>=2.33.1
