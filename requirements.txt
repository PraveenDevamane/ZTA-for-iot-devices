# BAZTA — Dashboard + Trust Engine Dependencies
# Install: pip install -r requirements.txt
# Compatible with Python 3.8 - 3.13

flask==3.1.3
flask-socketio==5.6.1
numpy==1.26.4
scikit-learn==1.5.2
joblib==1.4.2
pandas==2.2.3
# Note: SDN controller dependencies are separate (see sdn/requirements-sdn.txt)
# They must be installed on the Linux/Mininet VM only.
